import 'package:flutter/material.dart';

class Layout {
  static const ELEVATION = 5.0;
  static const ICONSIZE = 30.0;
  static const RADIUS = 15.0;
  static EdgeInsets borderRadiusAll = EdgeInsets.all(RADIUS);
  static const SPACING = 20.0;
  static EdgeInsets MARGINS_ALL = EdgeInsets.all(SPACING);
}
